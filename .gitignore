# User environment — never commit credentials
.env

# Runtime data — bind mounts (legacy, unused in MeshCentral deployment)
data/

# SQLite artifacts
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# Local backup archives
backups/
